// main.js — Entry point (IIFE modules, no ES modules needed)
(function() {
'use strict';
// Modules load via regular <script> tags in index.html
// All functions are global (window scope) via regular script loading
})();
